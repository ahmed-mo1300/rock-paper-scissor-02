// links _blank
const allLinksInPage = document.querySelectorAll("a");

allLinksInPage.forEach((link) => {
  link.setAttribute("target", "_blank");
});

// my slogan (Will be added later isa)
